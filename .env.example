# -----------------------------------------------------------------------------
# Environment variables for FreeFire Arsenal
#
# How to use:
# 1. For local development, create a copy of this file named `.env.local`.
# 2. Fill in the values in your `.env.local` file. This file is ignored by Git.
# 3. For deployment (e.g., Vercel), set these environment variables in your
#    project's settings on the hosting platform.
# -----------------------------------------------------------------------------

# Source URLs for the Free Fire item data (JSON files)
# These are used on the server-side to fetch the initial item list.
DATA_URL_1="https://raw.githubusercontent.com/iamaanahmad/FreeFireItems/refs/heads/main/FFiconDataOB47.json"
DATA_URL_2="https://raw.githubusercontent.com/iamaanahmad/FreeFireItems/refs/heads/main/FFiconData46.json"
DATA_URL_3="https://raw.githubusercontent.com/iamaanahmad/FreeFireItems/refs/heads/main/IconData.json"

# Image APIs
# These MUST be prefixed with NEXT_PUBLIC_ to be available on the client-side.
# Note: Do not include the query parameter part (e.g., ?id= or ?image=).
NEXT_PUBLIC_IMAGE_API_PRIMARY="https://freefireinfo.vercel.app/icon"
NEXT_PUBLIC_IMAGE_API_FALLBACK="https://system.ffgarena.cloud/api/iconsff"
